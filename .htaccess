<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si la requête ne cible pas déjà le dossier public/
    RewriteCond %{REQUEST_URI} !^/api/public/

    # On réécrit l'URL de manière transparente vers public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
